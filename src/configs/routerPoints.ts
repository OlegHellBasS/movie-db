export enum EPoints {
    BASE = '/',
    MOVIES_LIST = 'moviesList',
    MOVIE_ID = '/movieInfo/:movieId',
    FILTER = '/filter/:paramsId',
    SEARCH = '/search/:query',
    FAVORITE = '/favorite',
    NOT_FOUND_PAGE = '*',
}
